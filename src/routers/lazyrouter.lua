local Promise = require(script.Parent.Parent.utility.Promise)

return function()
    t ='/route/:id';
    t ='/route/:attribute';
    t ='route/132';
    return {
        params,
        route,
        callback
    }
end